# In app/proguard-rules.pro

# Add project specific ProGuard rules here.
# By default, the Chaquopy plugin will automatically add the rules it needs.

# This is useful for debugging stack traces.
-keepattributes SourceFile,LineNumberTable